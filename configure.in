AC_INIT(src/flexmem_cli.c)
AM_INIT_AUTOMAKE(flexmem, 0.7)
AM_CONFIG_HEADER(config.h)

AM_MAINTAINER_MODE

AC_ISC_POSIX
AC_PROG_CC
AC_PROG_CXX
AC_PROG_RANLIB
AC_HEADER_STDC

AM_PATH_GLIB(1.2.0)
AM_PATH_OPENOBEX(0.9.8)

AC_SUBST(CFLAGS)
AC_SUBST(CPPFLAGS)
AC_SUBST(LDFLAGS)

CFLAGS="$CFLAGS -Wall"

AC_OUTPUT([
Makefile 
src/libflexmem/libcobexbfb/Makefile
src/libflexmem/Makefile
src/Makefile])
